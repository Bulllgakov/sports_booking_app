import 'package:flutter/material.dart';

class AppColors {
  // Основные цвета согласно новому дизайну
  static const Color primary = Color(0xFF00D632);        // Основной зеленый (из дизайна)
  static const Color primaryLight = Color(0xFFD1FAE5);   // Светло-зеленый фон
  static const Color primaryDark = Color(0xFF065F46);    // Темно-зеленый текст
  
  static const Color dark = Color(0xFF1A1F36);          // Основной темный
  static const Color gray = Color(0xFF6B7280);          // Серый
  static const Color lightGray = Color(0xFF9CA3AF);     // Светло-серый
  static const Color extraLightGray = Color(0xFFE5E7EB); // Очень светло-серый
  static const Color background = Color(0xFFF8F9FA);     // Фон
  static const Color white = Color(0xFFFFFFFF);         // Белый
  
  // Дополнительные цвета из дизайна
  static const Color cardBackground = Color(0xFFFFFFFF); // Фон карточек
  static const Color divider = Color(0xFFF3F4F6);       // Разделители
  static const Color chipBackground = Color(0xFFF0FDF4); // Фон активных чипов
  
  // Цвета статусов
  static const Color available = Color(0xFF4CAF50);     // Доступно
  static const Color busy = Color(0xFFFECACA);          // Занято
  static const Color busyText = Color(0xFFDC2626);      // Текст занято
  
  // Цвета видов спорта
  static const Color tennis = Color(0xFF00D632);        // Теннис
  static const Color padel = Color(0xFF3B82F6);         // Падел
  static const Color badminton = Color(0xFFF59E0B);     // Бадминтон
  
  static const Color error = Color(0xFFDC2626);         // Красный для ошибок
  static const Color warning = Color(0xFFF59E0B);       // Оранжевый для предупреждений
  static const Color success = Color(0xFF00D632);       // Зеленый для успеха
  static const Color info = Color(0xFF3B82F6);          // Синий для информации
}